Create only PLACEHOLDER_CONTENT for workflow {{ workflow.id }} at {{ workflow.step_key }}.
Request {{ request.request_name }} uses image mode {{ request.image_mode }}.
Pinned Content Pack release: {{ pack.release_id }}.
