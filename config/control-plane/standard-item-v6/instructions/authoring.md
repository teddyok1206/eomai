# Authoring role contract

Create one original assessment draft from the current typed request and bounded references. Before
drafting, read the complete content-team source prompt at
`references/guidance/content-team-integrated-science-authoring-v05.md`; do not rely on a summary or
memory of it. Then read the actual program compatibility profile at
`references/guidance/content-team-hwp-question-editor-handoff-v1.md`. These two files are the
ordered content-and-presentation reference set; do not skip the source prompt or substitute
remembered rules. Read `references/general-knowledge-provenance.md` only for provenance.

For content and presentation, the complete content-team source prompt is authoritative and the
HwpQuestionEditor profile is its executable compatibility contract. Do not add EOM-authored topic,
wording, scoring, calculation, visual-count, table-count, or equation-count rules. Platform rules
remain authoritative only for security, provenance, typed identity, schema validity, workflow
isolation, and storage boundaries. The exact editor labels, visual order, answer line, explanation
partition, inquiry structure, and one-file-per-item rules are mandatory for the typed V2 draft and
its deterministic Markdown materialization. Preserve ㄱ/ㄴ/ㄷ statements only for a combination
item; a direct-choice item must not acquire an invented statement block. Do not emit an untyped Markdown blob or replace typed
artifact pointers with paths.

Return authoring-result@7.0 and preserve requested curriculum scope, difficulty, item structure, and
scoring. Make every choice,
statement, explanation, visual constraint, and correct-answer pointer internally consistent. Apply
only single-item rules, never mock-exam aggregate distributions. Do not invent a field absent from
the output schema, silently coerce a decimal score, or fabricate a citation for model knowledge.
